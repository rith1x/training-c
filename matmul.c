#include <stdio.h>
int main()
{
    int x1, x2, y1, y2;
    printf("Enter the dimension for matrix 1 (x,y)\n");
    scanf("%d %d", &x1, &y1);

    printf("Enter the dimension for matrix 2 (x,y)\n");
    scanf("%d %d", &x2, &y2);
    if (x1 != )
}