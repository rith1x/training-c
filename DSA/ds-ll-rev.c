#include <stdio.h>

int main(){

    next = null
    temp = *p
    while (temp->link != NULL){ // traverse
        next1 = temp->link // 
        temp->link = next 
        next = temp
        temp = next
    }
}